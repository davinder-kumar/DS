import React from 'react';
import { render } from '@testing-library/react';

const Input = (props) => {
    
    switch(props.type){
        case "input" :
            
    }

    return ()

}


export default Input;