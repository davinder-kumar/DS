import React from 'react';
import { render } from '@testing-library/react';

const Input = (props) => {
    let inputLe = 
    switch (props.type) {
        case "input":

            break;
    }

    return ()

}


export default Input;